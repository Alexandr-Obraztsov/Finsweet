import styled from "styled-components";
import {globalTheme} from "../../GlobalTheme";

export const Cap1 = styled.span`
    font-family: Inter, sans-serif;
    font-size: 16px;
    font-weight: 600;
    line-height: 20px;
    letter-spacing: 3px;
    text-align: left;
    color: ${globalTheme.colors.black};
    text-transform: uppercase;
`
