import styled from "styled-components";
import {globalTheme} from "../../GlobalTheme";

export const Cap3 = styled.span`
    font-family: Inter, sans-serif;
    font-size: 16px;
    font-weight: 500;
    line-height: 20px;
    letter-spacing: 3px;
    text-align: left;
    text-transform: uppercase;
    color: ${globalTheme.colors.black};
`
